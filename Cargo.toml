[package]
name = "harfrust-python"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
license = "MIT"

[lib]
name = "_harfrust"
crate-type = ["cdylib"]

[dependencies]
harfrust = { version = "0.5.2", features = ["std"] }
pyo3 = { version = "0.28.3", features = ["extension-module"] }